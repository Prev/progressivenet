{
  "name": "progressivenet",
  "version": "1.0.0",
  "description": "Framework for Progressive Deep Learning Models",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git://github.com/Prev/progressivenet.git"
  },
  "scripts": {
    "build": "tsc -d",
    "build-clean": "rm -rf dist && tsc -d",
    "index": "cd dist && node index.js",
    "test": "jest"
  },
  "author": "prevdev@gmail.com",
  "license": "UNLICENSED",
  "dependencies": {
    "@tensorflow/tfjs": "^1.7.4",
    "@tensorflow/tfjs-core": "^1.7.4",
    "uint-n-array": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "jest": "^25.2.4",
    "ts-jest": "^25.3.0",
    "typescript": "^3.9.7"
  }
}
